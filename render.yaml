services:
  - type: web
    name: oranthia-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
      - key: ADMIN_TOKEN
        sync: false
      - key: WORLD_GATE_LEVEL
        value: 25